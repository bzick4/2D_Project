using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FallingCheck : MonoBehaviour
{
   // private void OnTriggerEnter2D(Collider2D collision)
   // {
   //    if(collision.gameObject.layer.Equals("Ground"))
   //    {
   //       Debug.Log(Hero._samurai.velocity.y);
   //    }
   //
   //    if (collision.gameObject.layer.Equals("Ground") && Hero._samurai.velocity.y < -4)
   //    {
   //       Debug.Log("Damage");
   //    }
   // }
}
